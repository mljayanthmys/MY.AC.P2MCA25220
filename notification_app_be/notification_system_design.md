//stage 1

const express = require(express)
req app = express();
app.use=(express.json);
let app.get('/api_user/'), (req,res)=>({
res.status(200);
});
app.get('/api_user/id'),(req,res)=>({
const user = user.find(u => u.id == parse(req.param))
if(!user){
res.status(400).send (user not found);
}
});

app.post('/api/user'),(req,res)=>({
const user={
length = user+1,
user = req.body.name
}
});
user.push(newuser);
res.status(200);

app.put('/api user'),(req,res)=({
const user =(user.find)
})
